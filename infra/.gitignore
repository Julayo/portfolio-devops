# Terraform local dirs
.terraform/
.terraform.lock.hcl

# Terraform state (sensitive)
terraform.tfstate
terraform.tfstate.backup

# Sensitive user files
*.tfvars
crash.log

# IAM policy delivery artifacts (account-specific, keep out of public repo)
iam/
